apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sRequireSecureContext
metadata:
  name: require-secure-context-audit-zone
  labels:
    app.kubernetes.io/part-of: propdevelopment
spec:
  enforcementAction: deny
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]
    namespaces: ["audit-zone"]
  parameters:
    message: "runAsNonRoot и readOnlyRootFilesystem обязательны"
